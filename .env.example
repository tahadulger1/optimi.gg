# =============================================================================
# Optimi.gg - Environment Variables Example
# =============================================================================
# Bu dosya, projenin çalışması için gerekli environment variable'ları gösterir.
# Bu dosyayı ".env.local" olarak kopyalayın ve değerleri doldurun.
# .env.local dosyası asla git'e commit edilmemelidir!
# =============================================================================

# -----------------------------------------------------------------------------
# Genel Ayarlar
# -----------------------------------------------------------------------------

# Uygulama ortamı: development | production | test
NODE_ENV=development

# Site URL'si (tam URL)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# API Ayarları
# -----------------------------------------------------------------------------

# Backend API URL'si
# NOT: NEXT_PUBLIC_ prefix'i client-side'da görünür yapar.
# Hassas bilgi içeriyorsa bu prefix'i KULLANMAYIN!
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# API Secret Key (sadece server-side - NEXT_PUBLIC_ KULLANMAYIN!)
# Bu değer backend ile iletişimde authentication için kullanılır
API_SECRET_KEY=your-secret-key-here

# -----------------------------------------------------------------------------
# Authentication
# -----------------------------------------------------------------------------

# JWT Secret (en az 32 karakter)
JWT_SECRET=your-jwt-secret-key-minimum-32-characters

# Session Secret
SESSION_SECRET=your-session-secret-key

# Demo Credentials (SADECE DEVELOPMENT İÇİN!)
# Production'da bu değerleri ASLA tanımlamayın!
# DEMO_EMAIL=demo@optimi.gg
# DEMO_PASSWORD=demo-password-here

# -----------------------------------------------------------------------------
# Veritabanı (Eğer kullanılıyorsa)
# -----------------------------------------------------------------------------

# DATABASE_URL=postgresql://user:password@localhost:5432/optimi

# -----------------------------------------------------------------------------
# External Services
# -----------------------------------------------------------------------------

# Resim yükleme servisi (CloudFlare R2, AWS S3, vb.)
# UPLOAD_SERVICE_URL=https://your-upload-service.com
# UPLOAD_SERVICE_KEY=your-upload-service-key

# Analytics (opsiyonel)
# NEXT_PUBLIC_ANALYTICS_ID=your-analytics-id

# -----------------------------------------------------------------------------
# Rate Limiting
# -----------------------------------------------------------------------------

# API rate limit (dakika başına istek)
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_WINDOW_MS=60000

# -----------------------------------------------------------------------------
# Güvenlik
# -----------------------------------------------------------------------------

# CORS izin verilen origin'ler (virgülle ayrılmış)
CORS_ALLOWED_ORIGINS=http://localhost:3000

# CSP (Content Security Policy) - Production'da sıkılaştırın
# CSP_SCRIPT_SRC='self'
# CSP_STYLE_SRC='self' 'unsafe-inline'

# =============================================================================
# GÜVENLİK UYARILARI:
# =============================================================================
# 1. NEXT_PUBLIC_ ile başlayan değişkenler client bundle'a dahil olur
#    ve tarayıcıda görünür. Hassas bilgileri ASLA bu prefix ile tanımlamayın!
#
# 2. Bu dosyayı .env.local olarak kopyalayın, .env.local git'e eklenmez.
#
# 3. Production'da demo credentials'ları ASLA tanımlamayın.
#
# 4. Secret key'ler en az 32 karakter uzunluğunda olmalıdır.
#
# 5. API key'leri ve secret'ları düzenli olarak rotate edin.
# =============================================================================
